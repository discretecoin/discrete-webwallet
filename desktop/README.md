# Discrete Wallet Desktop (Windows)

This Electron wrapper runs the existing wallet UI locally without relying on a browser tab or hosted domain.

## Run

```bash
npm run desktop:start
```

## Build Portable EXE (Windows x64)

```bash
npm run desktop:dist
```

Output will be in `dist-desktop/`.
